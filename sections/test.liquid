
  <div>
    <p>Test Section ------------------------- -------------------------
      {{ section.settings.my_id }}
      {{ section.settings.my_second_id }}
    </p>
  </div>
  <div>
    {{ section.settings.collection.collection_reference }}
  </div>
  <div>
    {{ section.settings.product }}
  </div>
  <div>
    {{ section.settings.collection_list }}
  </div>
  <div>
    {{ section.settings.product.variants }}
  </div>
  <div>
    {{ section.settings.collection.all_types }}
  </div>

  <div>
    {{ section.settings.collection_list }}
  </div>

  <div style="background-color: {{ section.settings.background_color }}">
    <p style="color: {{ section.settings.text_color }} ">Test color</p>
  </div>

  {% for colection1 in section.settings.collection_list %}
    <div> {{ colection1.title }} </div>
  {% endfor %}

  {% for img_url in section.settings.product.images %}
    {{ img_url | image_url : width : 200 | image_tag }}
  {% endfor %}

{% for block in section.blocks %}
  {% case block.type %}
    {% when 'slider' %}
      <div {{ block.shopify_attributes }}>
        <p>Test Block</p>
      </div>
  {% endcase %}
{% endfor %}

{% schema %}
{
  "name": "Test Rich Text",
  "tag": "section",
  "class": "richtext",
  "settings": [
    {
      "type": "collection",
      "id": "collection",
      "label": "collection"
    },
    {
      "type": "collection_list",
      "id": "collection_list",
      "label": "collection list",
      "limit": 4
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#000000"
    },
    {
      "type": "color_background",
      "id": "background_color",
      "label": "Background",
      "default": "linear-gradient(#ffffff, #000000)"
    }
  ],
  "max_blocks": 2,
  "blocks": [
    {
      "name": "PiSlide cker",
      "type": "slider",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Image test"
        }
      ]
    },
    {
      "name": "PiSlide cker",
      "type": "slider",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Image test"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Slideshow",
      "blocks": [
        {
          "type": "slider"
        },
        {
          "type": "slider"
        }
      ]
    }
  ]
}
{% endschema %} 